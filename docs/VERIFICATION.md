# 验收记录 · 2026-09-09

部署地址：https://ai-cognit.com/harness/

- 首批 77 条资料，覆盖 9 个来源组织分组、17 个监测入口。
- 在生产服务器以专用用户完成实际抓取和 DeepSeek 提炼；17 个来源均成功访问。
- systemd timer 已启用，下一次运行：2026-09-10 08:00 北京时间（随机延迟最多 60 秒）。
- 浏览器实际操作：关键词搜索、公司与论文类型筛选、空结果重置、文章跳转、原文链接、收藏 / 取消、已读 / 未读、笔记填写与刷新保存、议题关注、阅读记录 JSON 导出和导入合并。
- 桌面首屏和 390 × 844 手机布局人工式浏览器检查；手机导航可用，无横向溢出。
- 生产页面没有记录到 JavaScript 或 CSP 错误；开发时的 HMR 重复 createRoot 警告通过分离 App 与入口修正。
- 10 项自动回归检查通过，生产构建通过，GitHub Actions 通过。
- 现有域名首页仍为 HTTP 200；Nginx 主站配置只增加 /harness 的 include。
- `/harness/.env`、`/harness/state/database.json`、`/harness/collector/update.py` 均返回 404。
- 服务端环境文件为 root:root 0600；GitHub 提交经过 key / token / 私钥模式扫描，没有实际凭证。

边界：阅读状态仅在当前浏览器保存。论文条目基于摘要，未做论文复现。来源抓取与模型输出会有失败，失败条目保留并退避重试；初始化期间的失败可在运行历史查看。当天初始资料回填队列仍会由后续每日任务继续处理，未声称已穷尽所有来源历史。

## 2026-09-09 · 开源项目与行业综合层

- 新增 Aider、OpenCode、Pi、Hermes、mini-SWE-agent、AutoGen、CrewAI、smolagents 八个项目，追踪固定 revision 的 README 与发布记录。
- 六个方向分别提供综合判断、共同机制、路线取舍、可执行验证问题；共性和取舍强制至少两个组织引用，拒绝不存在的引用。
- 新增测试覆盖跨组织约束、伪造引用拒绝、生成失败保留上一版、输入不变不重复生成、项目去重与组织平衡。
- 线上 Chromium 实测：方向入口、六张方向卡、三类综合段落、证据跳转、390px 手机布局无横向溢出，页面无 JavaScript 错误。锁屏期间使用独立 headless Chrome；未要求解锁。
- GitHub 匿名配额实际耗尽后，公开 Atom 回退成功获取全部 25 个信源。每日 08:00 北京时间的服务器 timer 仍启用。

## 2026-09-09 · Google 账号与私有收藏

- 旧版收藏核实为浏览器 localStorage，不是公共共享收藏。新版本未登录时不读取旧记录到当前收藏，也不返回服务器个人记录。
- Google 登录入口在线浏览器实测可打开官方登录页面，无 redirect_uri_mismatch；实际个人账号的 Google 验证需由本人完成。
- 21 项 Python 测试通过：包含两个独立账号的收藏与笔记隔离、匿名请求拒绝、CSRF Origin 拒绝、账号切换后旧标签页写入拒绝、退出撤销、伪造 state / ID token 拒绝、PKCE、浏览器绑定与一次性回调票据。
- 独立 Chrome 测试前端保存、退出清空、切换到第二账号无首个账号收藏（这部分 API 使用受控模拟）；后端隔离由真实本地 HTTP + SQLite 测试覆盖。手机 390px 无横向溢出，登录入口及页面无 JS 错误。
- 私有 SQLite 路径不在 Web 根目录，公开路径测试为 404；个人 API 与 OAuth 回调禁止缓存和访问日志。原网站首页仍返回 200。
- 发现既有 OAuth 回调域名证书已于 2026-08-16 过期，已成功续期并部署专用每日续期检查；对 ACME 的临时出站例外在执行后清理，不改变其他代理规则。续期后重新验证原 Cogni 回调路由和 Harness 回调路由。
